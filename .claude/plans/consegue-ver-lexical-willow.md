# Contexto

O usuário perguntou se consigo ver o output dos comandos bash executados localmente. A resposta é sim — vejo que `psql --version` retornou "command not found", confirmando que o PostgreSQL ainda não está instalado no sistema.

Não há implementação de código necessária aqui. A próxima ação é orientar o usuário na instalação do PostgreSQL no Windows.
